"""
domain/job/entities.py

Reserved for future child entities within the Job bounded context
(e.g. JobApplication, InterviewSlot).
The JobAggregate itself is the aggregate root and lives in aggregate.py.
"""
