"""
domain/resume/entities.py

Reserved for future child entities within the Resume bounded context
(e.g. ResumeVersion if we add versioning in Phase 3+).
The ResumeAggregate itself is the aggregate root and lives in aggregate.py.
"""
